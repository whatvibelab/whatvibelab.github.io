/* ==========================================================================
   analytics-config.js — 방문 통계(GA4) On/Off 스위치.
   gaId 가 비어 있으면(기본값) analytics.js 는 아무 것도 로드하지 않는다.
   woody가 Google Analytics 4 측정 ID(G-XXXXXXXXXX)를 발급받으면 아래 값만
   채우면 바로 켜진다. (ad-config.js 와 같은 방식)
   ========================================================================== */

window.ANALYTICS_CONFIG = {
  gaId: "", // 예: "G-ABCDE12345"
};
